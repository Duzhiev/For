const arr = ['Хьумид', 'Муслим', 'Адам', 'Ислам', 'Шарип', 'Артур', 'Байсангур', 'Мохьмад', 'Ибрагим', 'Салахь'];
let m = [];
for (let i = 0; i < arr.length; i++) {
    if(arr[i][0] === 'А') {
        m.push(arr[i])
    }
}
console.log(m)